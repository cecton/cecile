#!/bin/sh

# basically a clone of startx to workaround freeze with nix xorg.xinit package

export DISPLAY=:`fgconsole`
vt=vt`fgconsole`

Xorg $DISPLAY $vt &
xorg_pid=$!

sleep 0.5

~/.xinitrc

kill $xorg_pid
wait $xorg_pid
