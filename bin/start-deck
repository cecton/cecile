#!/bin/sh
set -x -e
mkdir -p /var/lib/overlays/usr/upper
mkdir -p /var/lib/overlays/usr/work
mount -t overlay overlay -o index=off,lowerdir=/usr,upperdir=/var/lib/overlays/usr/upper,workdir=/var/lib/overlays/usr/work /usr
pacman-key --init
pacman-key --populate
